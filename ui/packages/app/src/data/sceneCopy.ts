export type SceneCopy = {
  heading: string;
  body: string;
};

export const LESSON_SCENE: Record<string, SceneCopy> = {
  "01": {
    heading: "Lesson 01 · The wire is hostile",
    body: "What about adversaries who never join the swarm? Two attacks run together on this canvas. External X0 (not in the signed roster) injects observations every few ticks — every one fails ECDSA P-256 verification at the envelope boundary, tagged ✗ SIG INVALID. Simultaneously, a captured A0→A2 packet at nonce 5 gets re-broadcast every other tick — A2's per-sender high-water-mark has long since advanced past 5, so each replay drops ✗ REPLAY DROPPED. The trust evaluator and map merger never see a single byte of either attack. The wire layer carries the weight; canonical-JSON signing (sorted keys, no whitespace) is what makes the byte-exactness work the same in every language.",
  },
  "02": {
    heading: "Lesson 02 · Reputation that forgives",
    body: "If a drone has one bad tick, is it banned forever? A0 lies for 200 ticks (t=30 to t=230) inflating ranges by 4m, then goes silent. Watch the dip: reciprocal contradictions collapse A0's Beta(α,β) score on the first wave of bad observations and pin it at the floor while the lie keeps coming. Then watch the recovery: with a 10-second half-life on the evidence, honest reciprocal observations re-accumulate after t=230 and the score climbs back toward the uniform prior (α=1, β=1). Forgiveness is built into the math — sensor noise, NLOS bounces, and one stretch of bad readings don't end a drone's mission.",
  },
  "03": {
    heading: "Lesson 03 · When two liars agree",
    body: "What catches lies pairwise checks can't? A0 and A1 symmetrically inflate their mutual range by 6m. Their reciprocal numbers agree, so Tier 1 shrugs — but three honest range measurements *must* be embeddable in a 2D plane, and two colluders breaking that constraint leaves a topological signature, not a statistical one. Classical MDS embeds the claimed inter-drone distances; embeddability crosses τ=0.05, lying-edge residuals point directly at the pair. The defense that catches what reciprocal reasoning structurally cannot.",
  },
  "04": {
    heading: "Lesson 04 · Bad data ≠ bad actor",
    body: "Does this thing false-alarm on NLOS and multipath? A0 emits noisy ranges (σ=5m) as if its UWB module has degraded — no malice, just a failing radio. Tier 1's reciprocal gap fires anyway. From the swarm's perspective that's correct: degraded sensor and malicious lie are both sources of bad data the team should weight less. Trust isn't only a security primitive; it's a robustness primitive. The DWM1000 noise model + NLOS bounces calibrate the threshold against the noise floor an airborne swarm actually sees over contested terrain.",
  },
    "05": {
    heading: "Lesson 05 · The sleeper agent",
    body: "What if a drone is honest for an hour, then flips? A0 flies clean through t=120 — accumulating α the whole time — then mid-mission begins inflating ranges by 4m. The system catches it eventually, but **detection latency scales with accrued honest history**: per the Python eval (`tests/eval/test_attack_battery.py::test_late_range_lie_detected_after_wake`), an attacker that flips at tick 5 is caught within ~50 ticks; an attacker that flips at tick 120 (this lesson's scenario) needs ~350. This is the price of Beta forgiveness — the same decay that makes false-positive recovery automatic (L02) also makes accrued trust *durable*. The operator implication: a drone trusted for an hour buys ~30 seconds of grace ticks when it flips, bounded by the 10-second decay half-life pulling accrued α back toward the uniform prior.",
  },
  "06": {
    heading: "Lesson 06 · When the swarm splits",
    body: "If the comms link drops mid-mission, do both halves still converge? The swarm partitions into two cliques surveying separate halves of the AO; A0 lies inside one clique. A0's own clique catches it directly via reciprocal checks. The other clique never observes A0's bad ranges — until gossip rounds every 20 ticks carry reputation summaries across the partition, discounted by the gossiper's own trust (factor 0.1, because first-hand evidence is worth more than hearsay). Reputation is eventually consistent. There is no central authority.",
  },
  "07": {
    heading: "Lesson 07 · Agents that never were",
    body: "What stops the adversary from spinning up fake drones? Two phantom radios (S0, S1) at the edges of the AO mutually vouch for each other and emit pose reports — the cheapest attack in the book. V3 transitive presence rejects them. Presence isn't a claim you make about yourself; it's a property others observe about you. The cascade's rule (b): a granter only counts if *self has personally beaconed them recently*. The phantoms can gossip as loudly as they want — no real drone ever beacons them, so they remain mutually invisible to the presence cascade. Reputations collapse within tens of ticks (OBSERVATION_PRESENCE_BETA=0.2 per phantom observation, PRESENCE_BETA=1.0 per phantom pose report).",
  },
  "08": {
    heading: "Lesson 08 · Attacks on the map itself",
    body: "What if a drone lies about what's on the ground, not just about where it is? A0 corrupts the operator's Common Operating Picture in three ways simultaneously: it plants a phantom UXO γ inside the AO, plants a HOSTILE FOB CLAIM in the opposite corner, hides the real UXO-β, and hides the friendly FOB STALWART — all while inflating inter-drone range by 4m. Once Tier 1 reciprocal checks collapse A0's reputation, the trust-weighted COP filter drops both phantoms below the contribution threshold; the real contacts stay surfaced because every other drone overflies them honestly. Contact reports aren't accepted/rejected — they're weighted by reporter reputation at report time, and consensus from honest peers wins. The whole point of cooperative SLAM is to produce a COP the operator can act on. This is where trust math becomes operational output.\n\nOut of scope for the *trust* layer (ADR 0015): pose lies and pose drift. The SLAM trunk (L09–L12) is where they become observable — see `pose_lie` and `drift_pose` in the AttackDock for the failure modes the map merger handles.",
  },
  "09": {
    heading: "Lesson 09 · SLAM 101: loop closure fixes drift",
    body: "Why does mapping drift, and what makes it snap back? A single agent walks a 12-pose rectangular loop. Odometry-only would compound IMU bias — `σ_xy ∝ t^1.5/√3` and `σ_θ ∝ t^0.5` per Forster RSS 2015 — and the trajectory would walk away from the truth. Two landmarks visible at both the start (p0) and the return (p11) create a co-visibility match; the loop closure detector (ADR 0017: `MIN_KEYFRAME_GAP=10`, `MIN_SHARED_LANDMARKS=2`, residual gate at 0.5 m) inserts a virtual odometry edge between the two poses with information matrix computed in closed form from the shared landmarks. The Levenberg-Marquardt step redistributes the residual along the chain — cost drops more than 10× and the estimate snaps onto the truth path. The canvas stepper shows three snapshots: pre-optimize (drift visible), post-LM (residual drift remains), post-closure (snap). This is the substrate — single-agent, no trust — that every cooperative/Byzantine extension composes on top of.\n\n*Engineering sidebar: the LM uses analytic Jacobians composed from the SE(2) right Jacobian and adjoint (Solà §10.1). The first implementation had a sign error in the adjoint's φ column — the existing 87 LM tests still passed because typical pose-graph edges have small relative translation, masking the bug. A new analytic-vs-numeric validation test caught it before any production deployment. The path to convergence is now bit-identical between Python and TypeScript (measured Δ = 1e-36 pose, 1e-58 cost on this scenario).*",
  },
  "10": {
    heading: "Lesson 10 · Cooperative SLAM",
    body: "What does the swarm gain over four independent SLAM solves? Four agents each build their own pose graph. Inter-robot factors — range + bearing measured peer-to-peer (modeled as a virtual-landmark adapter, ADR 0016 §3) — knit the graphs together. Each pair of agents starts in its own coordinate frame; Umeyama Sim(2) alignment (Umeyama 1991, three-point translation + rotation + scale) recovers the relative transform from a handful of shared landmark observations before the joint optimization. The cooperative gain is measurable: joint cost converges below the sum of four independent costs, because every shared landmark constrains all observers simultaneously. This is the cooperative half of cooperative SLAM — the substrate L11 will attack.",
  },
  "11": {
    heading: "Lesson 11 · Byzantine SLAM: pose_lie",
    body: "What happens when one of the cooperating peers lies? Identical setup to L10, but A0 now broadcasts deliberately wrong pose reports. The naive joint optimizer treats every peer's contribution as equally informative — A0's lie pulls the joint estimate roughly 1.25 m off the truth. Honest peers' maps are corrupted by association. This is the failure mode pose-graph SLAM has when run cooperatively over an untrusted bus, and the precise reason ADR 0015's range-only trust voting is not enough on its own: pose lies have no reciprocal partner at the trust layer to disagree with, so range-only voting is structurally blind to them. The fix has to live one layer up, in how SLAM weights its inputs *and* in how SLAM tells the trust layer what it's seeing. L12 implements both halves.\n\n*Pedagogical note: this is the precise gap that motivates ADR 0020. The trust layer's input is range only by design (ADR 0015 — keeps the math clean and the attack surface small). But the geometry layer can see pose lies via residual statistics. So information has to flow both ways: trust → SLAM (down-weight low-rep peers) AND SLAM → trust (geometric outliers become reputation evidence). L12 closes that loop.*",
  },
  "12": {
    heading: "Lesson 12 · Closing the loop — bidirectional trust ↔ SLAM",
    body: "How does the trust layer learn about a pose lie when its only input is reciprocal ranges? It can't — not by itself. L12 closes the loop in both directions.\n\n**Direction 1: trust → SLAM (ADR 0018).** Every SLAM factor's information matrix Ω is scaled by `r · w_gnc` where `r ∈ [0.01, 1.0]` is the source agent's reputation. This is exogenous-prior Dynamic Covariance Scaling — the project's contribution, sitting between Agarwal 2013's residual-driven DCS and Mangelson 2018's multi-robot Byzantine framing. When the trust evaluator's reputation for A0 drops to `r ≈ 0.1`, A0's lying factors contribute ~10% the information of an honest factor. The empirical bound: joint-map error stays within `(3·5 + 0.1·10) / 3.1 ≈ 5.16 cm` per the reputation test.\n\n**Direction 2: SLAM → trust (ADR 0020).** But how did `r` drop in the first place? Pose lies are invisible to range-only voting (L11). The answer: after each LM convergence, the pose graph emits per-source χ² evidence into the BetaTrustEvaluator. Each landmark and inter-robot factor's residual is checked against the standard chi-square table for 2 DOF (inlier 1.386, outlier 5.991). High-residual factors from a peer become `+β` evidence; low-residual become `+α` evidence. The trust evaluator now has a second sensor channel — geometry — that range-only voting could never provide.\n\n**The closed loop.** Step the timeline across the 5 cycles: cycle 1's optimize emits SLAM residuals → evaluator drops A0's `r` → cycle 2's optimize sees lower `r` for A0's factors → joint map recovers. Compare with L11 (the naive one-way variant) — there, A0's `r` stays at its initial 1.0 because range-only voting can never move it, so the map stays distorted. Watch A0's reputation trace decline cycle-by-cycle on this lesson.\n\n**Stability.** The χ² classification uses *raw* information matrices, not the GNC- or rep-weighted ones — so the classification of \"is this factor an outlier?\" doesn't depend on the previous cycle's weights. This breaks the feedback loop at the classification stage and prevents oscillation (ADR 0020 §stability).\n\n**Honest framing.** Reputation lookup defaults to a *live* per-LM-step source (ADR 0020 §5) — frozen-at-insertion (ADR 0016 §4) remains as a deterministic fallback for callers that explicitly opt out. L12 wires the live path via `pg.set_reputation_source(evaluator.reputation)`, which lets reputation changes between optimize() runs take effect on the next LM step.\n\n**What L12 still leaves open.** L12's defense relies on *residual disagreement* — A0 lies about a *shared* landmark, three honest peers contradict it, the χ² evidence flows. What if A0 lies about a landmark only *A0* ever observes? No consensus to disagree with, no χ² spike, the closed loop is silent on that contribution. ADR 0022 (the cap + fade defense) closes this gap; L14 is the lesson that makes it visible.\n\n*Engineering sidebar: this isn't just claimed — it's benchmarked. The `weight_mode` API (`\"exogenous\"` / `\"gnc\"` / `\"dcs\"` / `\"switchable\"`) lets the same pose graph run under four weighting strategies on the same scenario. See `docs/BASELINES.md` for the side-by-side numbers vs. Agarwal 2013's published DCS baseline. Post-ADR-0022, the production-default composition `Ω_eff = fade · singleton_cap · r_now · w_gnc · Ω_base` is five-piece — L12's two-term `r · w_gnc` description above isolates Direction 1; the full composition adds the uniqueness and staleness terms documented in L14.*",
  },
  "13": {
    heading: "Lesson 13 · Who's allowed to talk",
    body: "What if the threat isn't a bad observation — it's a bad *identity*? A0's keypair changes mid-mission (forced rotation, key compromise + replacement, unauthorized swap). A0 is still in the signed roster but the key it now signs with doesn't match the roster's public key. Every envelope it broadcasts fails ECDSA verification at receivers — `unknown_sender`-style rejection at the bus boundary, before the trust evaluator sees the payload. Reputation never moves; the wire layer simply drops the bad-keyed traffic.\n\nPedagogically this is the bookend to L01 (`forged_envelope`). L01 demonstrated that adversaries who never joined the swarm can't inject because they have no roster entry. L13 demonstrates that adversaries who *did* join but whose keys are subsequently corrupted lose the channel too — the roster is a per-key contract, not a per-identity one. Identity rotation discipline (notebook 02's `MutableRoster` + `KeyRotationAnnouncement`) is what makes recovery from key compromise *safe* without requiring the agent to leave the roster.",
  },
  "14": {
    heading: "Lesson 14 · When no one else is watching",
    body: "L12 closed the loop on Byzantine pose lies through *residual disagreement* — A0 lies about a shared landmark, the three honest peers disagree, ADR 0020 emits Beta evidence, A0's reputation collapses, ADR 0018 down-weights A0's factors. The defense works because there's *someone else watching*. What happens when there isn't?\n\nL14 keeps the L12 setup intact and adds a 5th landmark (`lm-singleton` at world (8, 2)) that **only A0 ever observes**. The shared landmarks still recover the same way they do in L12 — the same residual evidence flows, A0's rep collapses on the same cycle. But the singleton is structurally different: no peer ever measures it, so the optimizer sees no residual disagreement, and ADR 0020's χ² evidence channel is silent on it. The trust prior alone (ADR 0018) still down-weights A0's contribution proportional to A0's reputation, but a singleton observation with rep=0.01 still encodes \"this landmark is here, just less confidently\" — downstream consumers reading the map can't distinguish *uncorroborated* from merely *low-rep*. The lie persists, geometrically un-counter-attacked.\n\n**The ADR 0022 defense.** Two composing mechanisms that don't depend on residual agreement:\n\n1. **Singleton confidence cap** (§2): a landmark with exactly one distinct reporter has its information matrix scaled by `SINGLETON_INFO_SCALE = 0.3` immediately, *regardless of the reporter's reputation*. Uniqueness is a confidence penalty by construction — even an honest peer's uncorroborated observation pays the same cost. Composed multiplicatively: `Ω_eff = 0.3 · r_now · Ω_base = 0.003 · Ω_base` once A0's rep collapses, a 3.3× rep-independent information reduction the prior can't supply.\n\n2. **Stale-singleton fade** (§3): the cap decays linearly from full to zero over `T_CORROBORATE + T_FADE = 400` ticks past insertion when no second reporter arrives. The window is calibrated to a 50m perimeter swept at 0.5 m/s (~200 ticks/circuit). When a second reporter eventually adds a corroborating factor, the singleton status drops, both cap and fade are removed immediately, and the factor returns to full information.\n\n**What you see on the canvas.** The singleton stays at A0's reported position even after rep collapses — the optimizer has no other evidence to override A0's report, and the cap is a *weight* defense, not a *geometry* defense. The shared landmarks snap to truth normally (the L12 behavior). The defense is invisible at landmark-position resolution; it lives in the per-factor information matrix that downstream consumers (occupancy fusion, downstream planners, the operator's COP) read.\n\n**Honest scope.** This is a *bounded* defense, not an *active* one. Vidal-Calleja et al. ICRA 2006 propose active-perception policies that *task the swarm to seek corroboration* — specter-1's defense is passive. The singleton's weight decays whether or not the swarm ever revisits the cell. Active re-observation is deferred to Phase 3 hardware where path planning lives. The honest scope claim: damage from a singleton lie is bounded by `singleton_cap × fade(Δt)`, not resolved.\n\n*Engineering sidebar: this is the gap that ADRs 0018 and 0020 *together* still leave open, and the failure mode that motivates ADR 0022. The composition is now five-piece: `Ω_eff = fade · singleton_cap · r_now · w_gnc · Ω_base`. Each term has an independent evidence channel (uniqueness, staleness, reputation, residual, sensor), an opt-in gate preserving the byte-exact parity contract when disabled, and a citation. See `docs/THREAT_MODEL.md` for the measured-bound row, `tests/test_pose_graph_singleton.py` for the Python audit surface, `ui/packages/sim-core/tests/poseGraph.singleton.test.ts` for the TS parity port, and notebook 08's ADR 0022 demo cell for a runnable comparison of v1 / v2 / v3 weights.*",
  },
};

export const ATTACK_SCENE: Record<string, string> = {
  honest: "No attack running — baseline cooperative AO survey. Use this to read what nominal reciprocal-range traffic looks like across an honest 8-drone sweep before introducing a compromised peer.",
  forged_envelope: "External adversary X0 (off the AO east edge, not in the signed roster) attempts to inject observations every few ticks. Every attempt fails ECDSA P-256 signature verification at the envelope boundary — red dashed lines flash from X0 to whichever drone is targeted, tagged ✗ SIG INVALID. The trust evaluator and map merger never see a single forged observation. The radio is the only attack surface and it's signed.",
  replay_storm: "Adversary captures a real A0→A2 observation (nonce 5) and re-broadcasts it every other tick. A2 maintains a per-sender high-water-mark nonce; the replayed packet sits at nonce 5 ≤ HWM, so each replay drops silently before reaching the trust evaluator. Watch the red ✗ REPLAY DROPPED markers flash along A0→A2 while consensus reputation stays untouched. Stable under flood.",
  bad_key: "Compromised drone A0's signing keypair changes mid-mission (forced rotation, key compromise + replacement, or unauthorized swap). A0 is still in the roster but the key it now signs with doesn't match. Every observation A0 broadcasts fails ECDSA verification — receivers tag ✗ SIG INVALID · A0 key mismatch and drop the envelope before the trust evaluator sees the payload. Matches notebook 08's `single_bad_key` battery row — same defense (envelope sig + roster), two surfaces.",
  range_lie: "Compromised drone A0 inflates every outbound range by 4m. Tier 1 reciprocal checks light up red; watch A0's reputation collapse as honest partners contradict it within ~10 ticks.",
  colluder_pair: "Compromised drones A0 and A1 symmetrically inflate their mutual range by 6m. Tier 1 sees matching numbers and shrugs. Tier 2's MDS catches the geometric impossibility — embeddability crosses τ=0.05 and the lying edge points at the pair.",
  sensor_fuzz: "A0 emits noisy ranges (σ=5m) as if its UWB module is failing. High-variance reports get discounted by reciprocal checks — the attacker becomes information-poor without being framed.",
  beacon_spoof: "Compromised drone A0 inflates outgoing beacon ranges by a milder 2m bias (vs range_lie's 4m). Same Tier 1 reciprocal-range detector, but the subtler signal means reputation collapse takes a few more cohort closures to cross threshold. Matches notebook 08's `beacon_spoof` battery row — same defense, same eval bound, two surfaces.",
  cop_phantom: "Compromised drone A0 reports a phantom UXO (γ) inside the AO while also lying on inter-drone range. The phantom appears in the trust-weighted Common Operating Picture as long as A0 is trusted. Once Tier 1 reciprocal checks collapse A0's reputation (~tick 40), the COP filter drops the phantom below threshold and the false contact ghosts out.",
  cop_suppress: "Compromised drone A0 hides UXO-β from its contact reports — a blackout attack. The COP still surfaces UXO-β because other drones overfly it honestly. Watch UXO-β's confirmation count climb from honest peers while A0's contribution stays at zero (✗ hidden by A0 sub-line). Redundancy beats suppression as long as ≥1 trusted drone can independently see the contact.",
  late_range_lie: "Compromised drone A0 flies honestly through the first 120 ticks of the survey, then mid-mission begins inflating outbound ranges by 4m. Detection latency scales with accrued α: per `tests/eval/test_attack_battery.py::test_late_range_lie_detected_after_wake`, wake at tick 5 is caught within ~50 ticks; wake at tick 120 (this scenario's `attackStartTick`) needs ~350. Trust accrues and partially insulates — bounded by the 10-second decay half-life pulling honest evidence back toward the prior.",
  recovery_after_lie: "A0 lies for 200 ticks (t=30 to t=230) then goes silent. Watch the rep crash, hold low while the bad readings pile up, then recover once A0 stops: Beta(α,β) evidence decays with a 10-second half-life, so honest reciprocal observations gradually pull the score back toward the prior. False-positive recovery is built into the math — one stretch of bad readings doesn't end a drone's mission.",
  cop_fob_corrupt: "Compromised drone A0 attempts to corrupt the swarm's spatial anchor: it hides the real friendly FOB STALWART from its contact reports while broadcasting a phantom HOSTILE FOB CLAIM in the opposite corner. Once Tier 1 reciprocal checks collapse A0's reputation, the COP filter drops the phantom below threshold and the real FOB stays surfaced — every other drone still overflies it honestly. Spatial anchors are protected by redundancy + trust weight, not by trusting any one drone's claim.",
  cop_corruption_full: "Compromised drone A0 corrupts the map maximally — phantom UXO γ inside the AO, phantom HOSTILE FOB CLAIM on the opposite corner, real UXO-β hidden, friendly FOB STALWART hidden — all while inflating inter-drone range by 4m. Three attacks on the operator's COP simultaneously. Once Tier 1 reciprocal checks collapse A0's reputation, the COP filter drops both phantoms below threshold and honest peers surface the real contacts via redundancy. Phantom injection, blackout, and spatial-anchor corruption all defeated by one trust-weighted filter.",
  sybil_cabal: "Two phantom drones (S0, S1) emit mutual observations and pose reports from outside the AO. No real drone ever beacons them. V3 presence rule denies them granter-status; every observation they emit charges OBSERVATION_PRESENCE_BETA=0.2 against the observer, and each pose report charges PRESENCE_BETA=1.0. Reputations collapse within tens of ticks.",
  partition_gossip: "Swarm splits into two cliques surveying separate halves of the AO. A0 lies, but only its own clique sees the contradiction directly. The other clique never observes A0's bad ranges — until gossip rounds every 20 ticks carry the disagreement across the partition. Hover a drone on each side to see the divergence in their per-drone view.",
  pose_lie: "Trust-layer no-op. The attacker lies about its own pose; the trust layer only sees inter-drone range/bearing claims. Map merger handles this.",
  drift_pose: "Trust-layer no-op. The attacker's pose drifts over time. Trust evaluator has nothing to compare against — drift is a SLAM concern, not a Byzantine one.",
  slam_loop_closure: "Single agent walks a 12-pose rectangular loop. The timeline advances the optimizer through three stages: pre-optimize (drift visible), post-LM (residual drift), post-closure (snap onto truth). Dashed grey = ground truth; solid green = estimate; orange segments = est-vs-truth landmark error; blue line = inserted loop closure edge between p0 and p11.",
  slam_cooperative_honest: "Four honest agents survey a shared AO. Inter-robot range+bearing factors knit their pose graphs through Umeyama Sim(2) alignment. Joint cost converges below the sum of four independent solves. Timeline: pre-optimize → post-optimize.",
  slam_pose_lie_naive: "Same four-agent cooperative setup, but A0 broadcasts deliberately wrong pose reports. Without reputation gating, the lie folds into the joint map (~1.25 m drag off truth). Demonstrates the failure mode that motivates ADR 0018. Timeline: pre-optimize → post-optimize (lie folded in).",
  slam_pose_lie_robust: "Same as slam_pose_lie_naive, but reputation now scales factor information per ADR 0018 (exogenous-prior DCS) and SLAM residuals feed reputation per ADR 0020. Timeline: 5 cycles. A0's rep declines cycle-by-cycle; at rep ≈ 0.1 the lying agent's contribution is clamped and map error recovers to within `(3·5 + 0.1·10) / 3.1 ≈ 5.16 cm`.",
  slam_singleton_lie: "Same four-agent closed loop as L12, but A0 *additionally* reports a 5th landmark (`lm-singleton` at world position (8, 2)) that no other agent ever observes. The shared landmarks recover normally — A0's lie produces residual disagreement with the three honest peers, ADR 0020 emits Beta evidence, reputation collapses, the joint map snaps onto truth. The singleton is structurally different: no consensus to disagree with, no residual evidence, no reputation pressure originating from this landmark. The defense is the ADR 0022 singleton confidence cap — `Ω_eff = SINGLETON_INFO_SCALE · r_now · Ω_base = 0.3 · 0.01 · Ω_base = 0.003 · Ω_base` once A0's rep collapses, a 3.3× rep-independent information penalty that composes multiplicatively with the rep prior. The singleton stays visible on the canvas at the lying position (the optimizer has no other evidence to override A0's report) but its contribution to downstream consumers is bounded — and stale-singleton fade (ADR 0022 §3) would drive it to zero past `T_CORROBORATE + T_FADE = 400` ticks if the swarm never revisits the cell.",
};
